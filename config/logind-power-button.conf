# Managed by cm5-local-scripts (GMBT-156)
# Short taps do nothing. A sustained >=5s press triggers poweroff.
# Rationale: boot-time panic-press loop on CM5 kiosks.
[Login]
HandlePowerKey=ignore
HandlePowerKeyLongPress=poweroff
PowerKeyIgnoreInhibited=yes
